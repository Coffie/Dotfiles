files:
  copy:
    - '<global>'
  symlink:
    - '<global>'

post_create:
  - '<global>'
  - poetry install

panes:
  - command: <agent>
    focus: true
  - split: vertical
    percentage: 30
  - command: "watchexec -e py -r -- poetry run pytest --tb=short -q"
    split: horizontal
