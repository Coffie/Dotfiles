files:
  symlink:
    - '<global>'
    - .venv/

panes:
  - command: <agent>
    focus: true
  - split: vertical
    percentage: 30
  - command: "watchexec -e py -r -- uv run pytest --tb=short -q"
    split: horizontal
