files:
  copy:
    - '<global>'
  symlink:
    - '<global>'

post_create:
  - '<global>'
  - uv sync --all-groups --all-extras

panes:
  - command: <agent>
    focus: true
  - split: vertical
    percentage: 30
  - command: "watchexec -e py -r -- uv run pytest --tb=short -q"
    split: horizontal
